oci_instances:
  hosts:
    ${instance.identifier}:
      ansible_host: ${instance.public_ip}
      ansible_port: 22
      ansible_python_interpreter: /usr/bin/python3
      ansible_ssh_private_key_file: ${var.private_ssh_key_path}
